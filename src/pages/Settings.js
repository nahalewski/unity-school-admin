import React from 'react';
import { Typography, Box } from '@mui/material';

function Settings() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Settings
      </Typography>
      <Typography variant="body1">
        Configure Unity School settings here
      </Typography>
    </Box>
  );
}

export default Settings;
